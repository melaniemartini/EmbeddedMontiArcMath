package fas.basicLibrary;

component Multiplication<T,N1 n=2> {
	port
		in T in1[n],
		out T out1;

	implementation Math{
	    T outVal=in(1);
	    for i=2:n
	        outVal=outVal*in(i);
	    end
	    out1=outVal;
	}
}
