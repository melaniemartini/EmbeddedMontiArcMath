package fas.basicLibrary;

component Max<T,N1 n=2> {
	port
		in T in1[n],
		out T out1;

	implementation Math{
	    T maxVal = in1(1);

	    for i=1:n
	        maxVal=max(maxVal,in1(i));
	    end
	    out1=maxVal;
	}
}
