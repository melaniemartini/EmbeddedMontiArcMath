package fas.basicLibrary;

component Or<T,N1 n=2> {
	port 
		in Boolean in1[n],
		out Boolean out1;

	implementation Math{
	    T outVal=in1(1);
	    for i=1:n
	        outVal=or(outVal,in(i));
	    end
	    out1=outVal;
	}
}
