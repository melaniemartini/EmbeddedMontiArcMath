package test;

component Addmatrix{
    port
        in (0 m : oo m) a[5],
        in (0 m : oo m) b[5],
        out (0 m : oo m) c[5];

    implementation Math{
        for x = 1:5
            c(x) = a(x) + b(x);
        end
    }

}
