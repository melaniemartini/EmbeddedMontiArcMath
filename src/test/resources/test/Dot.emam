package test;

component Dot{
    port
        in (0 m: oo m) a[5],
        in (0 m: oo m) b[5],
        out (0 m: oo m) c[5],
        out B sOut;

    implementation Math{
        c = a*a .* b;
    }

    connect true -> sOut;
}
